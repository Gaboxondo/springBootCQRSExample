server:
  port: 8090
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: backAccount
      username: sa
      password: sa
    kafka:
      producer:
        bootstrap-servers: localhost:9092
        key-serializer: org.apache.kafka.common.serialization.StringSerializer
